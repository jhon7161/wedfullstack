{
  "name": "my-express-app",
  "version": "1.0.0",
  "description": "A simple Express.js application with MongoDB integration",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "rmdir /s /q dist && cd ..\\FRONT && npm run build && xcopy /s /e dist ..\\backendjhon",
    "deploy:full": "npm run build && git add . && git commit -m uibuild && git push"

  },
  "author": "Tu Nombre",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.19.2",
    "mongoose": "^8.3.4",
    "morgan": "^1.10.0",
    "punycode": "^2.3.1",
    "url-encode-decode": "^1.0.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
